version: "3"

networks:
  apple-net:
    external: true
  banana-net:
    external: true
  orange-net:
    external: true

services:
  redis:
    image: redis
    ports:
      - "6379:6379"
    #volumes:
    #  - "/home/docker/data:/data"
    #deploy:
    #  placement:
    #    constraints: [node.role == manager]
    command: redis-server --appendonly yes
    networks:
      - apple-net
      - banana-net
      - orange-net
